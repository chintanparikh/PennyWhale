.row
  .col-lg-2
  .col-lg-8
    - flash.each do |type, message|
      .alert.fade.in.alert-dismissable{class: "alert-#{type}"}
        %button.close{data: {dismiss: 'alert'}} &times;
        = message
  .col-lg-2
